# JSON parser matching src/main.py JsonFormatter output (single-line JSON).
[PARSER]
    Name       app-json
    Format     json
    Time_Key   timestamp
    Time_Format %Y-%m-%dT%H:%M:%S.%LZ
    Time_Keep  On
